@*@model AcademicPortalProject.Models.StudentTable

    @{
        ViewBag.Title = "CourseEnrollment";
    }

    <h2>CourseEnrollment</h2>


    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">
            <input type="hidden" value="@ViewBag.StudentId" id="StudentId" name="StudentId" />
            <div class="form-group">
                <div class="col-md-10">
                    @Html.Label("Select Course")
                    @Html.DropDownList("CourseId", null, htmlAttributes: new { @class = "form-control" })
                </div>
            </div>

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Enroll" class="btn btn-default" />
                </div>
            </div>
        </div>
    }

    <div>
        @Html.ActionLink("Back to Homepage", "Homepage",new { id = Model.StudentId })
    </div>

    <script src="~/Scripts/jquery-1.10.2.min.js"></script>
    <script src="~/Scripts/jquery.validate.min.js"></script>
    <script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>*@

@model AcademicPortalProject.Models.StudentTable

@{
    ViewBag.Title = "CourseEnrollment";
}

<h2>CourseEnrollment</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <input type="hidden" value="@ViewBag.StudentId" id="StudentId" name="StudentId" />
        <div class="form-group">
            <div class="col-md-10">
                @Html.Label("Select Course")
                @Html.DropDownList("CourseId", null, htmlAttributes: new { @class = "form-control" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Enroll" class="btn btn-success" />
            </div>
        </div>
    </div>
}

    <div>
        @Html.ActionLink("Back to Homepage", "Homepage", new { id = Model.StudentId })
    </div>

<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script src="~/Scripts/jquery.validate.min.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>
